// Create a ToDo application

// create an empty array
// Add an event listener on button
// as long as the input is not "quit" the app should continue running
// use while() if() - elseif(), forEach, for(), push(), pop(), splice()